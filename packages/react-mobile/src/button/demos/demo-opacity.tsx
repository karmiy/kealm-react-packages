import React from 'react';
import { Button } from '@kealm/react-mobile';

export default () => {
    return (
        <Button type='primary' activeOpacity={0.7}>
            TouchableOpacity
        </Button>
    );
};
